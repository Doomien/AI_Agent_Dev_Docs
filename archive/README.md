# Archive

Historical artifacts kept for reference. **Not** active operational docs.

| Path | What it was | Superseded by |
|------|-------------|---------------|
| `v01/` | First-pass active execution state for this repo | n/a — abandoned approach |
| `v02/` | v2-era active execution state for this repo | Use `templates/` framework directly |
| `templates-v02/` | Framework v2.0 template set | `templates/` (was `templates/v03/`) |
| `templates-v2-framework/` | v2-era role-based templates (PM_Agent / IC_Agent two-role model, ContextChain, QUICK_REFERENCE, etc.) | `templates/WORKFLOW.md` — PM → IC → QA three-perspective model |

Do not adopt anything from this folder for new work. Cross-references from active docs into `archive/` should be removed when found.
